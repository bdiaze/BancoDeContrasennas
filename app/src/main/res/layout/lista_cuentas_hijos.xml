<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="3dp">

    <ImageView
        android:id="@+id/IV_claveExpirada"
        android:layout_width="25dp"
        android:layout_height="match_parent"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_warning_white_24dp"
        android:contentDescription="@string/contrCaducada"
        android:layout_marginStart="5dp"
        android:layout_marginEnd="10dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:id="@+id/LL_soporteTexto">

        <TextView
            android:id="@+id/TV_nombreCuenta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:maxLines="1"
            android:textColor="@color/letra"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/TV_descripcionCuenta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:textColor="@color/letraAtenuada"
            android:textSize="14sp" />
    </LinearLayout>

</LinearLayout>